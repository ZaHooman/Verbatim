# Verbatim
A real-time ASL-to-speech translator with audio-reactive visualization. Build with Mediapipe, OpenCV, Random Forest scikit-learn, TTS (Text-to-Speech) = pyttsx3, OSC (Open Sound Control) = python-osc, and TouchDesigner.
